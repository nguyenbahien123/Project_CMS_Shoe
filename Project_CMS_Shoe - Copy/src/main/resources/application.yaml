server:
  port: 8080
  servlet:
    context-path: /cms

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/cms_shoe"
    username: root
    password: sasasa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
